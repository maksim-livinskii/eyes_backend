{
  "name": "eyes",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "6.10.2"
  },
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd addon && npm install -g node-gyp && npm install && cd .. && node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.17.1",
    "express": "^4.15.2"
  }
}
